apiVersion: v1
kind: Service
metadata:
  name: {{ include "mypythonapp.fullname" . }}
  labels:
    {{- include "mypythonapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mypythonapp.selectorLabels" . | nindent 4 }}
