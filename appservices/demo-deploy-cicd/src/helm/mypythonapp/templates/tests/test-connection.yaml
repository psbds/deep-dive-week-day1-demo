apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mypythonapp.fullname" . }}-test-connection"
  labels:
{{ include "mypythonapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mypythonapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
